/**
 * Use this log instead of console.log, so that this can be changed to add custom logging if needed.
 */

export const log = {
  log: console.log,
  error: console.error,
  warn: console.warn,
  info: console.info,
  debug: console.debug,
  trace: console.trace,
  dir: console.dir,
}
